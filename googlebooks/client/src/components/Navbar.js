import React from "react";

function Navbar(props) {
    return (
        <div>Google Books Search
            <button>Search</button>
            <button>Saved</button>
        </div>
    )
}

export default Navbar;